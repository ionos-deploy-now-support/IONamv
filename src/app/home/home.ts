import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'amv-home',
  imports: [RouterLink],
  templateUrl: './home.html',
})
export class Home {}
